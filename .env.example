# Copia este archivo a ".env" y completa los valores.
#
# Este repo usa Poetry + FastAPI + 2 workers (generate_worker / print_worker).
# Los valores aquí son placeholders; NO subir secretos reales.

LOG_LEVEL=INFO

# --- DB (PostgreSQL) ---
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/savh_jobs_print

# --- Storage local ---
UPLOAD_DIR=data/uploads

# --- Worker polling ---
POLL_SECONDS=2

# --- Server ---
HOST=127.0.0.1
PORT=8000

# --- Observability (opcional) ---
# Métricas Prometheus en `http://HOST:PORT/metrics` (requiere instalar instrumentator).
ENABLE_METRICS=false
METRICS_PATH=/metrics

# Sentry (requiere instalar `sentry-sdk`)
SENTRY_DSN=
SENTRY_ENVIRONMENT=dev
SENTRY_TRACES_SAMPLE_RATE=0
SENTRY_PROFILES_SAMPLE_RATE=0
SENTRY_SEND_DEFAULT_PII=false

# --- Google Sheets (Service Account) ---
GOOGLE_APPLICATION_CREDENTIALS=/abs/path/to/service_account.json
SHEETS_ID=YOUR_SHEET_ID

CLIENTES_SHEET=CLIENTES
DESTINATARIOS_SHEET=DESTINATARIOS
VENTAS_SHEET=VENTAS
DETALLE_SHEET=DETALLE_VENTAS

CLIENTES_RANGE=A1:K
DESTINATARIOS_RANGE=A1:H
VENTAS_RANGE=A1:H
DETALLE_RANGE=A1:J

# --- Fecha / TZ ---
USE_CONFIG_DATE=false
CONFIG_DATE=2026-01-21
TIMEZONE=America/Santiago

# --- PDFs de salida ---
PDF_ORDERS_PATH=data/shipping_list/shipping_list.pdf
PDF_GUIDES_PATH=data/guides/guides.pdf

# --- Layout PDF ---
TITLE="EMPRESA SAVH INVERSIONES SPA"
SUBTITLE="Bodega Los Pinos"
MAX_ITEMS=5
CONTACT="Contacto: +56 9 0000 0000"
LOGO_PATH=

# --- Impresión (Windows + SumatraPDF) ---
PRINTER_NAME=YOUR_PRINTER_NAME
SUMATRA_PATH=C:\\Program Files\\SumatraPDF\\SumatraPDF.exe
